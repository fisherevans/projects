---
import { SOCIAL_LINKS } from '../consts';
const year = new Date().getFullYear();
---

<footer>
    <div class="inner">
        <span>&copy; {year} fisher evans</span>
        <span class="links">
            {SOCIAL_LINKS.map((link) => <a href={link.href}>{link.label}</a>)}
        </span>
    </div>
</footer>
<style>
    footer {
        border-top: 1px solid var(--rule);
        margin-top: 4em;
        color: var(--muted);
    }
    .inner {
        max-width: var(--max-width);
        margin: 0 auto;
        padding: 1.5em 1em;
        display: flex;
        justify-content: space-between;
        align-items: baseline;
        gap: 1em;
        font-size: 0.85rem;
        flex-wrap: wrap;
    }
    .links {
        display: flex;
        flex-wrap: wrap;
        gap: 1em;
        align-items: baseline;
    }
</style>
